Start > UnquotedString ">" choice Start .
Start > .
choice > continuation .
continuation > recogniser continuation .
continuation > "." .
recogniser > QuotedString .
recogniser > nonTerminal .
nonTerminal > UnquotedString .

{-
Requires these rules:

RULE pre UnquotedString |- I
RULE pre QuotedString |- I
RULE pre nonTerminal |- I

The above rules are omitted from this file since we cannot parse them using the grammar described in this file.
They are present in 'cfg', so we may do this:

amperspiegel -i cfg.ASL -asParser -i basic.cfg -showP -parse basic.cfg -apply cfg -showP -parse basic.cfg -apply cfg -showP

Since cfg is bootstrapped:

amperspiegel -Parse basic.cfg cfg -showP -parse basic.cfg -apply cfg -showP -parse basic.cfg -apply cfg -showP

amperspiegel -i cfg.ASL -asParser -i basic.cfg -parse basic.cfg -apply parser population bla -showP parser
amperspiegel -i cfg.ASL -asParser -i basic.cfg -show
amperspiegel -parse basic.cfg cfg -apply parser population bla -showP parser
-}